# GoatCounter

The static site uses the `homeprojectwise` GoatCounter property with the documented `count.v5.js` integration in the HTML `<head>`. The scheduled article template uses the same integration so new pages are tracked automatically.